# Exercício 3.2 - Complete a tabela abaixo, respondendo True ou False. Considere
# a = 4, b = 10, c = 5.0, d = 1 e f = 5.
                    # Expressão Resultado
                    # a == c,   True or False
                    # a < b,    True or False
                    # d > b,    True or False
                    # c! = f,   True or False
                    # a == b,   True or False
                    # c < d,    True or False
                    # b > a,    True or False
                    # c >= f,   True or False
                    # f >= c,   True or False
                    # c <= c,   True or False
                    # c <= f,   True or False
                    
a = 4
b = 10
c = 5.0
d = 1
f = 5

print('\n')

print('a == c? \t{}'.format(a == c))  # False
print('a <  b? \t{}'.format(a < b))   # True
print('d >  b? \t{}'.format(d > b))   # False
print('c != f? \t{}'.format(c != f))  # False
print('a == b? \t{}'.format(a == b))  # False
print('c <  d? \t{}'.format(c < d))   # False
print('b >  a? \t{}'.format(b > a))   # True
print('c >= f? \t{}'.format(c >= f))  # True
print('f >= c? \t{}'.format(f >= c))  # True
print('c <= c? \t{}'.format(c <= c))  # True
print('c <= f? \t{}'.format(c <= f))  # True

print('\n')